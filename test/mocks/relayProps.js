/* globals jest */

export const relayFragmentProp = {
}

export const relayRefetchProp = {
  refetch: jest.fn()
}

export const relayPaginationProp = {
  hasMore: jest.fn(),
  isLoading: jest.fn(),
  loadMore: jest.fn()
}
